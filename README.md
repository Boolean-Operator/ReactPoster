# Vite React Twitter Clone

- Created with Vite
- Build with:
  - React
  - React Router
  - CSS Modules
  - Express (Dummy Backend)

To run this example:

- `npm install` or `yarn`
- `npm run dev` or `yarn dev`
